---
phase: 06-performance-tests
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - package.json
  - .github/workflows/test-suite.yml
  - e2e/lighthouse/lighthouse.config.js
  - e2e/lighthouse/run-lighthouse.ts
autonomous: true
requirements:
  - PERF-02

must_haves:
  truths:
    - "npm run test:performance runs the Playwright perf suite (not Lighthouse)"
    - "CI has a performance-tests job that runs parallel to e2e and blocks PR on failure"
    - "Lighthouse files are removed from e2e/lighthouse/"
    - "The old lighthouse CI job no longer exists in test-suite.yml"
  artifacts:
    - path: "package.json"
      provides: "Updated test:performance script"
      contains: "playwright test --config=playwright.perf.config.ts"
    - path: ".github/workflows/test-suite.yml"
      provides: "performance CI job replacing lighthouse job"
      contains: "performance"
  key_links:
    - from: "package.json test:performance"
      to: "playwright.perf.config.ts"
      via: "npx playwright test --config"
      pattern: "playwright test --config=playwright.perf.config.ts"
    - from: ".github/workflows/test-suite.yml performance job"
      to: "npm run test:performance"
      via: "CI step calling script"
      pattern: "npm run test:performance"
---

<objective>
Update package.json script, replace the Lighthouse CI job with a Playwright-based performance job in GitHub Actions, and remove the old Lighthouse files.

Purpose: Wire the new performance suite into the CI pipeline so threshold breaches block PRs (PERF-02), and clean up the now-unused Lighthouse infrastructure.

Output: Updated package.json, updated test-suite.yml, removed Lighthouse files.
</objective>

<execution_context>
@D:/DEV/ProOps/.claude/get-shit-done/workflows/execute-plan.md
@D:/DEV/ProOps/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-performance-tests/06-CONTEXT.md
@.planning/phases/06-performance-tests/06-RESEARCH.md
@.planning/phases/06-performance-tests/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
playwright.perf.config.ts exists at project root with:
- testDir: './e2e/performance'
- reporter includes json output to performance-report/results.json
- webServer on port 3001 with full emulator env vars

From package.json (current):
```json
"test:performance": "npx tsx e2e/lighthouse/run-lighthouse.ts"
```

From .github/workflows/test-suite.yml (current):
- Lines 69-98: `lighthouse` job that runs `npx lhci autorun`
- Lines 11-67: `e2e` job
- Lines 100-160: `security` job
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json script and replace CI lighthouse job with performance job</name>
  <files>package.json, .github/workflows/test-suite.yml</files>
  <read_first>
    - package.json (find current test:performance script at line ~28)
    - .github/workflows/test-suite.yml (full file — understand structure of e2e job to replicate setup steps for performance job, identify lighthouse job lines 69-98 to replace)
  </read_first>
  <action>
**package.json change:**
Replace the `test:performance` script value from:
```
"npx tsx e2e/lighthouse/run-lighthouse.ts"
```
to:
```
"npx playwright test --config=playwright.perf.config.ts"
```

**.github/workflows/test-suite.yml change:**
Remove the entire `lighthouse:` job block (lines 69-98 approximately, from `lighthouse:` through the upload artifact step). Replace it with a `performance:` job:

```yaml
  performance:
    name: Performance Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install functions dependencies
        run: cd functions && npm ci

      - name: Build functions
        run: cd functions && npm run build

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Run performance tests
        run: npm run test:performance
        env:
          CI: true
          NEXT_PUBLIC_USE_FIREBASE_EMULATORS: 'true'
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: demo-proops-test
          NEXT_PUBLIC_FIREBASE_API_KEY: demo-key
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: demo-proops-test.firebaseapp.com
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: demo-proops-test.appspot.com
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: '000000000000'
          NEXT_PUBLIC_FIREBASE_APP_ID: '1:000000000000:web:demo'
          FIRESTORE_EMULATOR_HOST: '127.0.0.1:8080'
          FIREBASE_AUTH_EMULATOR_HOST: '127.0.0.1:9099'
          FIREBASE_STORAGE_EMULATOR_HOST: '127.0.0.1:9199'

      - name: Upload performance report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: performance-report
          path: performance-report/
          retention-days: 14
```

Key differences from the old lighthouse job:
- No `npm run build` for Next.js (webServer in config handles dev server startup)
- Functions build IS needed (emulator loads functions from functions/lib/)
- Playwright browsers + Firebase CLI installed (same as e2e job)
- No `needs:` — runs parallel to e2e and security (D-14)
- No `continue-on-error` — failure blocks PR (D-17)
- Same emulator env vars as the e2e job

Ensure the job is at the same indentation level as `e2e:` and `security:` jobs.
  </action>
  <verify>
    <automated>node -e "const pkg = require('./package.json'); if (pkg.scripts['test:performance'].includes('playwright')) { console.log('PASS: script updated'); process.exit(0); } else { console.log('FAIL'); process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - package.json `test:performance` value is `npx playwright test --config=playwright.perf.config.ts`
    - package.json does NOT contain `run-lighthouse.ts` in any script
    - test-suite.yml does NOT contain `lighthouse` as a job name
    - test-suite.yml does NOT contain `lhci autorun`
    - test-suite.yml contains `performance:` as a job name
    - test-suite.yml contains `npm run test:performance` in a step
    - test-suite.yml performance job contains `FIRESTORE_EMULATOR_HOST`
    - test-suite.yml performance job contains `npm install -g firebase-tools`
    - test-suite.yml performance job contains `npx playwright install chromium --with-deps`
    - test-suite.yml performance job has `timeout-minutes: 15`
    - test-suite.yml performance job does NOT have a `needs:` key (parallel per D-14)
  </acceptance_criteria>
  <done>package.json script points to Playwright perf config; CI workflow has performance job parallel to e2e, no lighthouse job remains</done>
</task>

<task type="auto">
  <name>Task 2: Remove Lighthouse files</name>
  <files>e2e/lighthouse/lighthouse.config.js, e2e/lighthouse/run-lighthouse.ts</files>
  <read_first>
    - e2e/lighthouse/lighthouse.config.js (confirm this is the only file — verify no other files in directory)
    - e2e/lighthouse/run-lighthouse.ts (confirm no other code imports from this directory)
  </read_first>
  <action>
Delete the following files:
- `e2e/lighthouse/lighthouse.config.js`
- `e2e/lighthouse/run-lighthouse.ts`

Then remove the `e2e/lighthouse/` directory itself (it should be empty after file deletion).

Before deleting, verify no other file in the codebase imports from `e2e/lighthouse/`:
```bash
grep -r "e2e/lighthouse" --include="*.ts" --include="*.js" --include="*.yml" --include="*.yaml" --include="*.json" .
```

If any imports are found besides the ones being removed (package.json script already updated in Task 1, test-suite.yml already updated), address them.

Also check if `@lhci/cli` is in devDependencies of package.json. If so, leave it for now (other projects or future phases may use it, and it causes no harm as an unused dep). Do NOT remove it — that is a separate cleanup concern.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); if (!fs.existsSync('e2e/lighthouse')) { console.log('PASS: lighthouse dir removed'); process.exit(0); } else { console.log('FAIL: directory still exists'); process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - e2e/lighthouse/ directory does not exist
    - e2e/lighthouse/lighthouse.config.js does not exist
    - e2e/lighthouse/run-lighthouse.ts does not exist
    - No remaining references to `e2e/lighthouse` in package.json scripts
    - No remaining references to `e2e/lighthouse` in test-suite.yml
  </acceptance_criteria>
  <done>Lighthouse files and directory removed; no dangling references remain in package.json or CI workflow</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> emulators | Performance job runs against Firebase emulators in CI, no production exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | D (Denial of Service) | CI performance job | accept | 15-minute timeout prevents runaway; parallel execution means failure doesn't block other jobs |
</threat_model>

<verification>
```bash
# Verify script is correct
node -e "console.log(require('./package.json').scripts['test:performance'])"
# Should output: npx playwright test --config=playwright.perf.config.ts

# Verify lighthouse files are gone
ls e2e/lighthouse/ 2>&1 || echo "Directory removed"

# Verify CI workflow structure
grep -n "performance:" .github/workflows/test-suite.yml
grep -n "lighthouse:" .github/workflows/test-suite.yml || echo "No lighthouse job (correct)"

# Full integration test
npm run test:performance
```
</verification>

<success_criteria>
- `npm run test:performance` executes Playwright tests (not Lighthouse)
- CI workflow has `performance` job, no `lighthouse` job
- e2e/lighthouse/ directory is gone
- Performance job runs parallel to e2e (no `needs:` dependency)
- Performance job failure blocks PR merge
</success_criteria>

<output>
After completion, create `.planning/phases/06-performance-tests/06-02-SUMMARY.md`
</output>
