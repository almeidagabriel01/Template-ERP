---
phase: 02-auth-multitenant
plan: 03
type: execute
wave: 1
depends_on: [02-02]
files_modified:
  - e2e/auth/tenant-isolation.spec.ts
autonomous: true
requirements: [AUTH-06]
gap_closure: true

must_haves:
  truths:
    - "Backend API isolation test asserts only 403 or 404 — no 502 fallback accepted"
    - "Stale comment claiming Functions emulator is not started is removed"
  artifacts:
    - path: "e2e/auth/tenant-isolation.spec.ts"
      provides: "4-test AUTH-06 suite with strict backend API assertion"
      contains: "expect([403, 404]).toContain"
  key_links:
    - from: "e2e/auth/tenant-isolation.spec.ts"
      to: "e2e/global-setup.ts"
      via: "Functions emulator started via --only auth,firestore,storage,functions"
      pattern: "expect\\(\\[403, 404\\]\\)\\.toContain"
---

<objective>
Close AUTH-06 gap: the backend API tenant isolation test at line 84 of `e2e/auth/tenant-isolation.spec.ts` accepts `[403, 404, 502]`, making the assertion vacuous when the Functions emulator is absent. Verification found this was a documentation/code drift: `global-setup.ts` already starts the Functions emulator (`--only auth,firestore,storage,functions`) and blocks setup until port 5001 is serving. The fix is to tighten the assertion to `[403, 404]` and remove the stale comment that misleads future readers.

Purpose: Remove a passing-but-vacuous test assertion. When 502 is removed, a genuine Functions emulator failure would cause a hard test failure rather than a silent pass — which is the correct behavior.
Output: `e2e/auth/tenant-isolation.spec.ts` with strict 403/404 assertion and accurate inline documentation.
</objective>

<execution_context>
@D:/DEV/ProOps/.claude/get-shit-done/workflows/execute-plan.md
@D:/DEV/ProOps/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/DEV/ProOps/.planning/PROJECT.md
@D:/DEV/ProOps/.planning/ROADMAP.md
@D:/DEV/ProOps/.planning/STATE.md
@D:/DEV/ProOps/.planning/phases/02-auth-multitenant/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten backend API isolation assertion and remove stale comment</name>
  <read_first>
    - e2e/auth/tenant-isolation.spec.ts (full file — confirm current assertion at line 84 and the comment block above it at lines 79-83)
    - e2e/global-setup.ts (lines 121-131 — confirm functions is in the --only flag)
  </read_first>
  <files>e2e/auth/tenant-isolation.spec.ts</files>
  <action>
    Two edits to `e2e/auth/tenant-isolation.spec.ts`:

    **Edit 1 — remove stale comment block (lines 79-83):**

    Delete these lines in the "backend API blocks cross-tenant proposal update" test:
    ```
    // 502 is accepted when the Functions emulator is not running (test env limitation).
    // The Next.js proxy targets http://127.0.0.1:5001/erp-softcode/... but the Playwright
    // global-setup only starts auth,firestore,storage emulators (not functions).
    // Tenant isolation at the API layer is verified in full-stack integration environments.
    // The Firestore-level isolation tests above cover the same security guarantee.
    ```

    Replace with a single accurate comment:
    ```
    // Functions emulator is started in global-setup (--only auth,firestore,storage,functions).
    // 403 = tenantId mismatch caught by Express middleware; 404 = document not found after tenant filter.
    ```

    **Edit 2 — tighten the assertion (line 84):**

    Change:
    ```typescript
    expect([403, 404, 502]).toContain(response.status());
    ```

    To:
    ```typescript
    expect([403, 404]).toContain(response.status());
    ```

    No other changes to the file. Do not modify the other 3 tests.
  </action>
  <verify>
    <automated>
      grep -n "502" e2e/auth/tenant-isolation.spec.ts
    </automated>
    The grep above must return no output (zero matches). Also verify the replacement is in place:
    <automated>
      grep -n "expect(\[403, 404\])" e2e/auth/tenant-isolation.spec.ts
    </automated>
    This must match exactly once.
  </verify>
  <acceptance_criteria>
    - `grep -n "502" e2e/auth/tenant-isolation.spec.ts` returns no output
    - `grep -c "expect(\[403, 404\])" e2e/auth/tenant-isolation.spec.ts` outputs `1`
    - `grep -n "global-setup only starts auth,firestore,storage" e2e/auth/tenant-isolation.spec.ts` returns no output (stale comment removed)
    - `grep -n "Functions emulator is started in global-setup" e2e/auth/tenant-isolation.spec.ts` matches once (accurate comment in place)
    - File still has exactly 4 tests (no tests added or removed)
  </acceptance_criteria>
  <done>The backend API isolation assertion no longer accepts 502. The comment accurately documents the environment. If the Functions emulator ever fails to start, global-setup throws and the test run aborts — the tightened assertion acts as a secondary guard confirming the emulator is serving.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → Functions emulator | Playwright request fixture calls /api/backend/* which proxies to Functions emulator at :5001 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | tenant-isolation.spec.ts backend API test | accept | Removed vacuous 502 fallback — test now fails hard if emulator unreachable, exposing infrastructure regressions |
</threat_model>

<verification>
After the edit, run:

```bash
grep -n "502" e2e/auth/tenant-isolation.spec.ts
# Must return: (no output)

grep -n "expect(\[403, 404\])" e2e/auth/tenant-isolation.spec.ts
# Must return: one match in the backend API test

npx playwright test e2e/auth/tenant-isolation.spec.ts
# Manual run (requires emulators started with seed): all 4 tests must pass
```

The human verification step (running Playwright against live emulators) is the definitive check that the tightened assertion passes against the running Functions emulator.
</verification>

<success_criteria>
- `e2e/auth/tenant-isolation.spec.ts` contains no reference to 502
- The backend API isolation test asserts `expect([403, 404]).toContain(response.status())`
- The stale comment claiming Functions emulator is not started is removed
- The file comment accurately reflects that global-setup starts functions
- AUTH-06 gap from 02-VERIFICATION.md is closed: backend API assertion is no longer vacuous
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-multitenant/02-03-SUMMARY.md` following the summary template at `@D:/DEV/ProOps/.claude/get-shit-done/templates/summary.md`.

Key items to document in the SUMMARY:
- Decision: 502 removed because global-setup.ts already starts Functions emulator (lines 125-126: `--only auth,firestore,storage,functions`); the 02-02 SUMMARY narrative was outdated
- Files modified: `e2e/auth/tenant-isolation.spec.ts` only
- Requirements completed: AUTH-06 (gap closed)
</output>
