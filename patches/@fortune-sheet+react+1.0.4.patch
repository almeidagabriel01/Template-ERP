diff --git a/node_modules/@fortune-sheet/react/dist/index.esm.js b/node_modules/@fortune-sheet/react/dist/index.esm.js
index c4486a2..11cb138 100644
--- a/node_modules/@fortune-sheet/react/dist/index.esm.js
+++ b/node_modules/@fortune-sheet/react/dist/index.esm.js
@@ -3356,6 +3356,12 @@ var SheetOverlay = function SheetOverlay() {
   var _useDialog = useDialog(),
     showDialog = _useDialog.showDialog;
   var containerRef = useRef(null);
+  var mouseMoveGuardRef = useRef({
+    x: null,
+    y: null,
+    isUpdating: false,
+    lastTs: 0
+  });
   var bottomAddRowInputRef = useRef(null);
   var dataVerificationHintBoxRef = useRef(null);
   var _useState = useState(""),
@@ -3422,12 +3428,36 @@ var SheetOverlay = function SheetOverlay() {
     }
   }, [debouncedShowLinkCard]);
   var onMouseMove = useCallback(function (nativeEvent) {
-    setContext(function (draftCtx) {
-      overShowLinkCard(draftCtx, refs.globalCache, nativeEvent, containerRef.current, refs.scrollbarX.current, refs.scrollbarY.current);
-      handleOverlayMouseMove(draftCtx, refs.globalCache, nativeEvent, refs.cellInput.current, refs.scrollbarX.current, refs.scrollbarY.current, containerRef.current, refs.fxInput.current);
-    });
+    var guard = mouseMoveGuardRef.current;
+    var buttons = nativeEvent.buttons || 0;
+    if ((buttons & 1) === 0 && (buttons & 2) === 0) {
+      return;
+    }
+    var now = Date.now();
+    if (now - guard.lastTs < 16) {
+      return;
+    }
+    var x = nativeEvent.clientX;
+    var y = nativeEvent.clientY;
+    if (guard.isUpdating || guard.x === x && guard.y === y) {
+      return;
+    }
+    guard.x = x;
+    guard.y = y;
+    guard.isUpdating = true;
+    try {
+      setContext(function (draftCtx) {
+        overShowLinkCard(draftCtx, refs.globalCache, nativeEvent, containerRef.current, refs.scrollbarX.current, refs.scrollbarY.current);
+        handleOverlayMouseMove(draftCtx, refs.globalCache, nativeEvent, refs.cellInput.current, refs.scrollbarX.current, refs.scrollbarY.current, containerRef.current, refs.fxInput.current);
+      });
+      guard.lastTs = now;
+    } finally {
+      guard.isUpdating = false;
+    }
   }, [overShowLinkCard, refs.cellInput, refs.fxInput, refs.globalCache, refs.scrollbarX, refs.scrollbarY, setContext]);
   var onMouseUp = useCallback(function (nativeEvent) {
+    var guard = mouseMoveGuardRef.current;
+    guard.isUpdating = false;
     setContext(function (draftCtx) {
       try {
         handleOverlayMouseUp(draftCtx, refs.globalCache, settings, nativeEvent, refs.scrollbarX.current, refs.scrollbarY.current, containerRef.current, refs.cellInput.current, refs.fxInput.current);
diff --git a/node_modules/@fortune-sheet/react/dist/index.js b/node_modules/@fortune-sheet/react/dist/index.js
index e404551..ad4bba0 100644
--- a/node_modules/@fortune-sheet/react/dist/index.js
+++ b/node_modules/@fortune-sheet/react/dist/index.js
@@ -1708,7 +1708,7 @@ var LinkEditCard = function LinkEditCard(_ref) {
         setLinkAddress(core.getRangetxt(context, context.currentSheetId, context.luckysheet_select_save[len - 1], ""));
       }
     }
-  }, [context, selectingCellRange]);
+  }, [context.currentSheetId, context.luckysheet_select_save, selectingCellRange]);
   if (!isEditing) {
     return /*#__PURE__*/React__default['default'].createElement("div", _objectSpread2(_objectSpread2({}, containerEvent), {}, {
       onKeyDown: function onKeyDown(e) {
@@ -2213,6 +2213,7 @@ var NotationBoxes = function NotationBoxes() {
     setContext = _useContext.setContext,
     refs = _useContext.refs;
   var flowdata = core.getFlowdata(context);
+  var lastPsShownCellsRef = React.useRef("");
   React.useEffect(function () {
     if (flowdata) {
       var psShownCells = [];
@@ -2229,6 +2230,13 @@ var NotationBoxes = function NotationBoxes() {
           }
         }
       }
+      var nextSignature = psShownCells.map(function (item) {
+        return "".concat(item.r, "_").concat(item.c);
+      }).join("|");
+      if (lastPsShownCellsRef.current === nextSignature) {
+        return;
+      }
+      lastPsShownCellsRef.current = nextSignature;
       setContext(function (ctx) {
         return core.showComments(ctx, psShownCells);
       });
@@ -3164,12 +3172,12 @@ var RangeDialog = function RangeDialog() {
     setRangeTxt2(function (r) {
       if (context.luckysheet_select_save) {
         var range = context.luckysheet_select_save[context.luckysheet_select_save.length - 1];
-        r = core.getRangetxt(context, context.currentSheetId, range, context.currentSheetId);
-        return r;
+        var nextRangeTxt = core.getRangetxt(context, context.currentSheetId, range, context.currentSheetId);
+        return r === nextRangeTxt ? r : nextRangeTxt;
       }
-      return "";
+      return r === "" ? r : "";
     });
-  }, [context, context.luckysheet_select_save]);
+  }, [context.currentSheetId, context.luckysheet_select_save]);
   return /*#__PURE__*/React__default['default'].createElement("div", {
     id: "range-dialog",
     onClick: function onClick(e) {
@@ -3367,6 +3375,12 @@ var SheetOverlay = function SheetOverlay() {
   var _useDialog = useDialog(),
     showDialog = _useDialog.showDialog;
   var containerRef = React.useRef(null);
+  var mouseMoveGuardRef = React.useRef({
+    x: null,
+    y: null,
+    isUpdating: false,
+    lastTs: 0
+  });
   var bottomAddRowInputRef = React.useRef(null);
   var dataVerificationHintBoxRef = React.useRef(null);
   var _useState = React.useState(""),
@@ -3433,12 +3447,36 @@ var SheetOverlay = function SheetOverlay() {
     }
   }, [debouncedShowLinkCard]);
   var onMouseMove = React.useCallback(function (nativeEvent) {
-    setContext(function (draftCtx) {
-      overShowLinkCard(draftCtx, refs.globalCache, nativeEvent, containerRef.current, refs.scrollbarX.current, refs.scrollbarY.current);
-      core.handleOverlayMouseMove(draftCtx, refs.globalCache, nativeEvent, refs.cellInput.current, refs.scrollbarX.current, refs.scrollbarY.current, containerRef.current, refs.fxInput.current);
-    });
+    var guard = mouseMoveGuardRef.current;
+    var buttons = nativeEvent.buttons || 0;
+    if ((buttons & 1) === 0 && (buttons & 2) === 0) {
+      return;
+    }
+    var now = Date.now();
+    if (now - guard.lastTs < 16) {
+      return;
+    }
+    var x = nativeEvent.clientX;
+    var y = nativeEvent.clientY;
+    if (guard.isUpdating || guard.x === x && guard.y === y) {
+      return;
+    }
+    guard.x = x;
+    guard.y = y;
+    guard.isUpdating = true;
+    try {
+      setContext(function (draftCtx) {
+        overShowLinkCard(draftCtx, refs.globalCache, nativeEvent, containerRef.current, refs.scrollbarX.current, refs.scrollbarY.current);
+        core.handleOverlayMouseMove(draftCtx, refs.globalCache, nativeEvent, refs.cellInput.current, refs.scrollbarX.current, refs.scrollbarY.current, containerRef.current, refs.fxInput.current);
+      });
+      guard.lastTs = now;
+    } finally {
+      guard.isUpdating = false;
+    }
   }, [overShowLinkCard, refs.cellInput, refs.fxInput, refs.globalCache, refs.scrollbarX, refs.scrollbarY, setContext]);
   var onMouseUp = React.useCallback(function (nativeEvent) {
+    var guard = mouseMoveGuardRef.current;
+    guard.isUpdating = false;
     setContext(function (draftCtx) {
       try {
         core.handleOverlayMouseUp(draftCtx, refs.globalCache, settings, nativeEvent, refs.scrollbarX.current, refs.scrollbarY.current, containerRef.current, refs.cellInput.current, refs.fxInput.current);
