---
phase: 16-lia-seguranca-billing
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/ui/progress.tsx
autonomous: true
requirements: [AIBI-01, AIBI-03, AIBI-06]

must_haves:
  truths:
    - "Shadcn Progress component is installed and importable"
    - "Free plan tenant calling chat endpoint gets 403 with code AI_FREE_TIER_BLOCKED"
    - "Tenant at limit gets 429 with resetAt field in response body"
    - "LiaInputBar disables when isAtLimit is true"
    - "Firestore rules block client writes to aiUsage and aiConversations"
    - "Firestore rules allow authenticated tenant reads on aiUsage"
    - "Firestore rules restrict aiConversations reads to owning user"
  artifacts:
    - path: "src/components/ui/progress.tsx"
      provides: "Shadcn Progress primitive for AiUsageCard"
  key_links: []
---

<objective>
Install the Shadcn Progress component (prerequisite for AIBI-04 in Plan 03) and verify that AIBI-01, AIBI-03, and AIBI-06 are already fully implemented -- no code changes needed for these three requirements.

Purpose: Wave 0 dependency setup. AIBI-04 (Plan 03) needs `Progress` from `src/components/ui/progress.tsx` which does not currently exist. Additionally, three of six phase requirements (AIBI-01, AIBI-03, AIBI-06) are already implemented in the codebase and need only verification, not implementation.

Output: `src/components/ui/progress.tsx` installed; verification evidence for AIBI-01, AIBI-03, AIBI-06.
</objective>

<execution_context>
@D:/DEV/ProOps/.claude/get-shit-done/workflows/execute-plan.md
@D:/DEV/ProOps/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-lia-seguranca-billing/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Shadcn Progress component</name>
  <files>src/components/ui/progress.tsx</files>
  <read_first>
    - src/components/ui/card.tsx (confirm Shadcn component pattern used in this project)
  </read_first>
  <action>
Run `npx shadcn@latest add progress` from the project root to install the Progress component.

This generates `src/components/ui/progress.tsx` with a Radix UI based progress bar primitive. The component is needed by Plan 03 (AiUsageCard) for the AI usage progress bar on the billing page.

After installation, verify the file exists and exports correctly by running `npx tsc --noEmit` to confirm no type errors.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/progress.tsx` file exists
    - `src/components/ui/progress.tsx` contains `export` and `Progress`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Progress component installed and importable from `@/components/ui/progress`</done>
</task>

<task type="auto">
  <name>Task 2: Verify AIBI-01, AIBI-03, AIBI-06 are already implemented</name>
  <files></files>
  <read_first>
    - functions/src/ai/chat.route.ts (lines 50-72: free tier 403 and limit 429 checks)
    - src/components/lia/lia-input-bar.tsx (isAtLimit prop disables input)
    - firestore.rules (lines 430-446: aiUsage and aiConversations rules)
  </read_first>
  <action>
Verify -- do NOT modify any files. These three requirements are already implemented:

**AIBI-01 (Free tier 403):**
Confirm `functions/src/ai/chat.route.ts` lines 51-57 contain:
- `if (planProfile.tier === "free")` guard
- Response: `res.status(403).json(...)` with `code: "AI_FREE_TIER_BLOCKED"`

**AIBI-03 (Limit 429 + UI disabled):**
Confirm `functions/src/ai/chat.route.ts` lines 62-72 contain:
- `if (!limitCheck.allowed)` guard
- Response: `res.status(429).json(...)` with `code: "AI_LIMIT_EXCEEDED"` and `resetAt` field
Confirm `src/components/lia/lia-input-bar.tsx` has `isAtLimit` prop that disables the input.

**AIBI-06 (Firestore rules):**
Confirm `firestore.rules` contains:
- `match /tenants/{tenantId}/aiUsage/{month}` with `allow read: if isAuthenticated() && belongsToTenant(tenantId)` and `allow write: if false`
- `match /tenants/{tenantId}/aiConversations/{sessionId}` with `allow read:` checking `resource.data.uid == request.auth.uid` and `allow write: if false`

Report findings. If any check fails, flag as a gap.
  </action>
  <verify>
    <automated>cd functions && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `functions/src/ai/chat.route.ts` contains `AI_FREE_TIER_BLOCKED` (AIBI-01)
    - `functions/src/ai/chat.route.ts` contains `AI_LIMIT_EXCEEDED` (AIBI-03)
    - `functions/src/ai/chat.route.ts` contains `resetAt` in 429 response body (AIBI-03)
    - `src/components/lia/lia-input-bar.tsx` contains `isAtLimit` prop (AIBI-03)
    - `firestore.rules` contains `aiUsage` match with `allow write: if false` (AIBI-06)
    - `firestore.rules` contains `aiConversations` match with `resource.data.uid == request.auth.uid` (AIBI-06)
  </acceptance_criteria>
  <done>AIBI-01, AIBI-03, AIBI-06 confirmed as already implemented with no code changes needed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Wave 0 installs a UI primitive and verifies existing code -- no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-00 | N/A | N/A | accept | No new attack surface in Wave 0 -- only installing a UI component and reading existing code |
</threat_model>

<verification>
1. `src/components/ui/progress.tsx` exists and contains `Progress` export
2. `npx tsc --noEmit` passes (frontend)
3. `cd functions && npx tsc --noEmit` passes (backend)
4. AIBI-01, AIBI-03, AIBI-06 grep checks all pass
</verification>

<success_criteria>
- Progress component installed and importable
- Three already-implemented requirements (AIBI-01, AIBI-03, AIBI-06) confirmed present in codebase
- TypeScript compilation passes for both frontend and backend
</success_criteria>

<output>
After completion, create `.planning/phases/16-lia-seguranca-billing/16-01-SUMMARY.md`
</output>
